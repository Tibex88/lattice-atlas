__pycache__/
.DS_Store
artifacts/metadata-cache/
artifacts/dashboard.sqlite3
artifacts/dashboard.sqlite3-shm
artifacts/dashboard.sqlite3-wal
data/

# Ignore the nested repo content by default.
src/

# Keep the root archive in the outer repo.
!data.tar.gz

# Keep the moved nested repo metadata out of the outer repo.
src/.git.bak/
.venv
